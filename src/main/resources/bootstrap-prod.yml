spring:
  application:
    name: api-gateway
  cloud:
    kubernetes:
      config:
        enabled: true
        name: api-gateway
        namespace: prod
        sources:
          - namespace: prod
            name: alaitp-shared
          - namespace: prod
            name: alaitp-api-gateway

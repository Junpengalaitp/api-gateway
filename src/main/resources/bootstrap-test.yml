# --spring.cloud.bootstrap.name=bootstrap-local-dev
spring:
  application:
    name: api-gateway
  cloud:
    kubernetes:
      config:
        enabled: true
        name: api-gateway
        namespace: local-dev
        sources:
          - namespace: local-dev
            name: alaitp-shared
          - namespace: local-dev
            name: alaitp-api-gateway
